using Avalonia.Controls;
using YouTubeGUI.Controls;

namespace YouTubeGUI.Commands
{
    public class IRElementCommand : CommandBase
    {
        public override void Execute(object? parameter)
        {
            /*switch (parameter)
            {
                case ItemsRepeaterElementPreparedEventArgs irepea:
                    if (irepea.Element is Border border)
                    {
                        WebImage wi = border.Child
                            .FindControl<WebImage>("WIThumbnail");
                    }
                    break;
            }*/
        }
    }
}